# Docs_GraphRAG
Leveraging SourceKit-LSP, Graphiti (+ Neo4j), TBD Vector DB, and Agno + LLM Providers in a GraphRAG workflow to write documentation.

# 11-30

Switching to directly using the compiled symbol graph and FalkorDB (built for GraphRAG) + Agno or Falkor's AI SDK

Not using graphiti since it uses LLM-based data ingestion, but I already have JSON